// Tool-specific formatters — thin re-exports from the shared utility layer.
// Keeps all tool components pointing to a single source of truth.
export { formatCurrency, formatPercent } from '@/lib/utils'
