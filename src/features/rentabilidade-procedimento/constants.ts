// ── Defaults de formulário ─────────────────────────────────────────────

export const DEFAULT_FIXED_COST_ALLOCATION = 0
export const DEFAULT_DESIRED_PROFIT_MARGIN_PERCENT = 30

// ── Limites de validação ───────────────────────────────────────────────

export const MIN_PROCEDURE_PRICE = 0.01
export const MIN_PROCEDURE_VARIABLE_COST = 0
export const MIN_PROCEDURE_DURATION_MINUTES = 1
export const MIN_HOURLY_CLINICAL_COST = 0.01
export const MIN_FIXED_COST_ALLOCATION = 0

export const MIN_DESIRED_PROFIT_MARGIN_PERCENT = 0
export const MAX_DESIRED_PROFIT_MARGIN_PERCENT = 95

// ── Precisão de arredondamento ─────────────────────────────────────────

export const PRECISION = 2
