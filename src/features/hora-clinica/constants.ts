// ── Defaults de formulário ─────────────────────────────────────────────

export const DEFAULT_WORKING_DAYS_PER_MONTH = 20
export const DEFAULT_HOURS_PER_DAY = 8
export const DEFAULT_OCCUPANCY_RATE_PERCENT = 70
export const DEFAULT_DESIRED_MONTHLY_PROFIT = 0

// ── Limites de validação ───────────────────────────────────────────────

export const MIN_WORKING_DAYS_PER_MONTH = 1
export const MAX_WORKING_DAYS_PER_MONTH = 31

export const MIN_HOURS_PER_DAY = 1
export const MAX_HOURS_PER_DAY = 24

export const MIN_OCCUPANCY_RATE_PERCENT = 1
export const MAX_OCCUPANCY_RATE_PERCENT = 100

export const MIN_MONTHLY_COST = 0
export const MIN_DESIRED_MONTHLY_PROFIT = 0

// ── Precisão de arredondamento ─────────────────────────────────────────

export const PRECISION = 2
